# План подготовки к собеседованию: Junior Backend Developer

## 1. Цель и формат

- Цель: выйти на уверенный уровень Junior Backend Developer и пройти техническое собеседование.
- Горизонт: 18 недель (можно ускорить до 12 или растянуть до 24).
- Режим: 5-7 часов в неделю.
- Результат: портфолио из практических мини-проектов + готовые ответы на ключевые вопросы.

## 1.1 Зафиксированные параметры

- Стек: `Node.js 20+` + `Express`.
- Фокус: backend.
- Дополнительное требование: в каждом модуле понимать, как backend-решение влияет на фронтенд.
- Целевой тип работодателя: компании среднего размера (mid-size).
- NestJS: **отложен** до уверенного владения Express и архитектурой.

## 2. Трек обучения (от базы к собеседованию)

### 2.1 Основы backend и HTTP

#### Теория — занятие 1 (до написания кода)

**Протокол HTTP** — текстовый протокол запрос/ответ поверх TCP. Клиент отправляет запрос, сервер возвращает ответ. Соединение без сохранения состояния (stateless).

**Структура HTTP-сессии:**
```
# Запрос
POST /tasks HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer <token>

{"title": "Buy milk"}

# Ответ
HTTP/1.1 201 Created
Content-Type: application/json
Location: /tasks/42

{"id": 42, "title": "Buy milk"}
```

**Заголовки (headers)** — метаданные запроса/ответа в формате `Имя: Значение`:
- `Content-Type` — формат тела (`application/json`, `text/html`, `multipart/form-data`)
- `Authorization` — токен доступа
- `Accept` — какой формат ответа принимает клиент
- `Cache-Control` — директивы кэширования
- `Set-Cookie` / `Cookie` — управление сессией
- `CORS-заголовки` — `Access-Control-Allow-Origin` и др.

**Cookies** — механизм хранения небольших данных на клиенте; браузер автоматически отправляет их с каждым запросом к домену.

Формат заголовка `Set-Cookie`:
```
Set-Cookie: sessionId=abc123; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=3600
```

| Атрибут | Назначение |
|---------|----------|
| `HttpOnly` | JS не может читать куку — защита от XSS-кражи токена |
| `Secure` | Кука передаётся только по HTTPS |
| `SameSite=Strict` | Кука не отправляется с других доменов — полная защита от CSRF |
| `SameSite=Lax` | Кука отправляется при навигации (GET), но не при кросс-сайтовых POST/AJAX |
| `SameSite=None` | Кука отправляется всегда (требует `Secure`) — для сторонних сервисов |
| `Expires` / `Max-Age` | Срок жизни. **Без них — сессионная кука** (удаляется при закрытии браузера) |
| `Domain`, `Path` | Область действия куки |

**Типы cookies:**
- **Session cookie** — без `Expires`/`Max-Age`, живёт до закрытия браузера.
- **Persistent cookie** — хранится на диске, задан срок. Используется для «Запомни меня».
- **HttpOnly cookie** — закрыта от JS, безопасно хранить session token или JWT.
- **Third-party cookie** — установлена чужим доменом (аналитика, реклама). Блокируется по умолчанию в Safari/Firefox.

**Cookies vs Web Storage:**

| | Cookie | localStorage | sessionStorage |
|--|--------|-------------|----------------|
| Отправляется с запросами | Автоматически | Нет | Нет |
| Доступ из JS | Только без `HttpOnly` | Да | Да |
| Срок жизни | Настраиваемый | Вечно (до очистки) | Вкладка |
| Размер | ~4 КБ | ~5–10 МБ | ~5–10 МБ |
| Уязвимость к XSS | `HttpOnly` защищает | Уязвим | Уязвим |

**URL и его части:**
```
https://api.example.com:443/tasks/42?sort=asc&page=1#section
  │      │               │   │         │              │
scheme  host            port path      query        fragment
```
- `path` — иерархический идентификатор ресурса
- `query string` — параметры фильтрации/пагинации; символы `пробел`, `&`, `=`, кириллица — **обязательно кодируются** через percent-encoding (`%20`, `%26`)
- `fragment` — обрабатывается браузером, на сервер не доходит

**Percent-encoding vs Base64:**
- **Percent-encoding** (`%XX`) — нужен в URL; заменяет символы, недопустимые или зарезервированные в URL
- **Base64** — нужен когда двоичные или произвольные данные нужно передать через текстовый канал, который не поддерживает произвольные байты:
  - `Authorization: Basic dXNlcjpwYXNz` (user:pass в base64)
  - данные изображений/файлов внутри JSON (`"avatar": "data:image/png;base64,..."`)
  - JWT-токен состоит из трёх base64url-кодированных частей (header.payload.signature)

> Важно: base64 — это **кодирование**, не шифрование. Данные легко декодировать.

**Версии протокола:**
- HTTP/1.1 — одно соединение, pipeline (редко), keep-alive
- HTTP/2 — мультиплексирование потоков, сжатие заголовков (HPACK)
- HTTP/3 — поверх QUIC/UDP, устойчивость к packet loss

#### Пререквизиты (утилиты, софт, библиотеки)
- Git
- VS Code
- Postman или Insomnia
- curl
- Node.js 20+
- Express 4/5
- `nodemon`

#### Практикум: ручной HTTP (Node.js, строка за строкой)

1. Создай `server.js` (минимальный сервер на встроенном `http`):

```js
const http = require("http");

const server = http.createServer((req, res) => {
  let body = "";
  req.on("data", (chunk) => (body += chunk));
  req.on("end", () => {
    console.log("---- REQUEST ----");
    console.log(req.method, req.url, "HTTP/" + req.httpVersion);
    console.log(req.headers);
    console.log("body:", body || "<empty>");
    console.log("-----------------");

    res.statusCode = 200;
    res.setHeader("Content-Type", "application/json");
    res.end(JSON.stringify({ ok: true, method: req.method, url: req.url }));
  });
});

server.listen(8080, () => {
  console.log("Server: http://127.0.0.1:8080");
});
```

2. Запусти сервер:

```bash
node server.js
```

3. Создай `manual-http-client.js` (TCP-клиент для ручного ввода HTTP):

```js
const net = require("net");
const readline = require("readline");

const socket = net.createConnection({ host: "127.0.0.1", port: 8080 }, () => {
  console.log("Connected to 127.0.0.1:8080");
  console.log("Type HTTP lines manually");
  console.log("/end -> send empty line");
  console.log("/quit -> close client");
});

socket.on("data", (data) => process.stdout.write(data.toString()));
socket.on("end", () => console.log("\nConnection closed by server"));
socket.on("error", (err) => console.error("Socket error:", err.message));

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
  terminal: true,
});

rl.on("line", (line) => {
  if (line === "/quit") {
    socket.end();
    rl.close();
    return;
  }
  if (line === "/end") {
    socket.write("\r\n");
    return;
  }
  socket.write(line + "\r\n");
});
```

4. Запусти клиент во втором терминале:

```bash
node manual-http-client.js
```

5. Введи `GET` руками (каждая строка + Enter):

```http
GET /hello HTTP/1.1
Host: 127.0.0.1:8080
Connection: close
/end
```

6. Введи `POST` руками (проверь `Content-Length`):

```http
POST /tasks HTTP/1.1
Host: 127.0.0.1:8080
Content-Type: application/json
Content-Length: 13
Connection: close
/end
{"x":"test"}
```

7. Сравни, что отправил клиент, с тем, что сервер вывел в `req.headers` и `body`.

#### План практических занятий
- Изучить структуру HTTP-запроса/ответа через `curl -v`.
- Ручным вводом отправить `GET` и `POST` через `manual-http-client.js`.
- Поднять простой HTTP-сервис с 3-5 endpoint.
- Реализовать `GET`, `POST`, `PUT`, `DELETE`.
- Вернуть корректные HTTP-статусы и JSON-ошибки.
- Добавить валидацию входных данных.
- Добавить CORS-настройку для тестового фронтенд-клиента.
- Добавить версионирование API (`/v1/tasks`).
- Реализовать пагинацию (`?page=1&limit=20`) и сортировку (`?sort=createdAt:desc`).
- Сгенерировать OpenAPI/Swagger-документацию для API (`swagger-jsdoc` + `swagger-ui-express`).

#### План вопросов
- Что происходит между клиентом и сервером при одном HTTP-запросе?
- Из каких частей состоит URL? Что кодируется и почему?
- Зачем нужен Base64 и чем он отличается от шифрования?
- Что такое REST и в чем его ограничения?
- Чем `PUT` отличается от `PATCH`?
- Когда использовать `200`, `201`, `204`, `400`, `404`, `409`, `422`, `500`? [См. ответ A1](#a1-http-статусы)
- Зачем версионировать API? Какие подходы (URL path, header, query)?
- Offset-пагинация vs cursor-пагинация — плюсы и минусы? [См. ответ A12](#a12-пагинация)
- Что такое middleware в Express и как работает цепочка `next()`?
- Чем `HttpOnly` кука отличается от обычной? Какие атрибуты куки защищают от XSS и CSRF?
- Чем `SameSite=Strict` отличается от `SameSite=Lax`?
- Где лучше хранить токен аутентификации: cookie или localStorage — и почему?

### 2.2 Async Node.js и Event Loop

> Это **самая частая** тема на собеседовании Node.js junior.

#### Теория

**Event Loop** — механизм, который позволяет Node.js обрабатывать тысячи одновременных соединений в одном потоке. Порядок фаз:

```
timers          ← setTimeout, setInterval
pending I/O     ← системные колбэки
idle, prepare   ← внутреннее
poll            ← I/O-колбэки (файлы, сеть, БД)
check           ← setImmediate
close           ← socket.on('close')

Между каждой фазой: microtask queue (Promise.then, queueMicrotask)
                  + process.nextTick (ещё до microtask)
```

**Эволюция асинхронности:**
- Callbacks → Promises → async/await
- `Promise.all()` — параллельно ждать все (падает при первом reject)
- `Promise.allSettled()` — параллельно, но собрать все результаты (успех+ошибки)
- `Promise.race()` — первый результат (используется для тайм-аутов)

**Блокировка event loop:** тяжёлая синхронная операция (JSON.parse огромного файла, crypto, цикл) замораживает весь сервер.

**EventEmitter** — базовый паттерн Node.js. Модули `http`, `net`, `fs`, `stream` наследуют от `EventEmitter`. Позволяет подписываться на события и публиковать их:

```js
const { EventEmitter } = require("events");
const emitter = new EventEmitter();
emitter.on("order:created", (order) => console.log("new order", order.id));
emitter.emit("order:created", { id: 42 });
```

**Streams** — абстракция для работы с данными чанками, без загрузки всего в память:
- `Readable` — источник (файл, HTTP-запрос, результат запроса к БД)
- `Writable` — приёмник (файл, HTTP-ответ)
- `Transform` — преобразование (gzip, CSV-парсер)
- `.pipe()` — соединяет потоки: `fs.createReadStream(file).pipe(gzip).pipe(res)`

Без Streams: весь файл читается в RAM за раз. Со Streams: данные идут чанками, память не растёт линейно с размером файла.

#### Пререквизиты
- Node.js 20+
- Умение работать с `--inspect` и Chrome DevTools

#### План практических занятий
- Написать примеры, показывающие порядок выполнения: `console.log` vs `setTimeout(0)` vs `Promise.resolve().then()` vs `process.nextTick()`.
- Переписать callback-код на Promises, затем на async/await.
- Реализовать параллельную загрузку данных из 3 источников через `Promise.all`.
- Написать пример блокирующей операции и измерить, как она влияет на обработку запросов.
- Обработка ошибок: что происходит при unhandled rejection и uncaught exception.
- Создать собственный EventEmitter: объявить событие, подписаться, эмитить.
- Прочитать большой файл через `fs.createReadStream` с `.pipe()` в response, не загружая в память целиком.

#### План вопросов
- Опиши фазы event loop в Node.js. [См. ответ A13](#a13-event-loop)
- Чем отличается microtask от macrotask?
- Какой порядок вывода: `setTimeout(0)` vs `Promise.resolve().then()` vs `process.nextTick()`?
- Что произойдёт, если выполнить `while(true)` в серверном коде?
- Чем `Promise.all` отличается от `Promise.allSettled`?
- Как Node.js обрабатывает тысячи соединений в одном потоке?
- Что такое EventEmitter и где он используется в ядре Node.js?
- Что такое Stream и зачем он нужен вместо загрузки всего файла в RAM?
- Как работает `pipe()` и что происходит при ошибке в потоке?

### 2.3 Язык, структуры данных, алгоритмическая база

#### Пререквизиты (утилиты, софт, библиотеки)
- JavaScript (ES2022+)
- Jest
- `eslint` + `prettier`
- LeetCode / Codewars (Easy-уровень)

#### Паттерны задач

| Паттерн | Пример задачи |
|---------|------------------|
| Frequency map | Two Sum, подсчёт символов, анаграммы |
| Two pointers | Палиндром, слияние отсортированных массивов |
| Sliding window | Макс. сумма подмассива длины k |
| Stack | Проверка скобок, min stack |
| Sorting | Сортировка объектов, кастомный comparator |
| Recursion | Факториал, flatten массива, обход дерева |

#### План практических занятий
- Решить 12 задач (2 на каждый паттерн) с LeetCode Easy.
- Для каждого решения записать сложность O(time) и O(space).
- Написать Jest-тесты с edge-case: пустой ввод, один элемент, дубликаты, отрицательные числа.
- Разобрать сложность `O(1)`, `O(log n)`, `O(n)`, `O(n log n)`, `O(n²)` на примерах.
- Написать и покрыть тестами утилитарные функции.

#### План вопросов
- Что такое временная и пространственная сложность?
- Когда использовать hash map вместо list/array?
- Почему тесты на edge-case важны? [См. ответ A2](#a2-edge-cases)
- Какая сложность у поиска в объекте по ключу vs в массиве по значению?

### 2.4 Git и командная работа

#### Пререквизиты
- Git 2.x
- GitHub аккаунт

#### План практических занятий
- Освоить базовый цикл: `add`, `commit`, `push`, `pull`, `status`, `log`, `diff`.
- Работа с ветками: `branch`, `checkout`, `merge`, `rebase` (интерактивный).
- Создать Pull Request на GitHub, провести code review своего кода.
- Разрешить merge-конфликт вручную.
- Настроить `.gitignore`, понять конвенцию коммитов (conventional commits: `feat:`, `fix:`, `chore:`).
- Настроить `npm` проект: `package.json` scripts, `semver` (`^` vs `~`), `npm audit`.

#### План вопросов
- Чем `merge` отличается от `rebase`?
- Что такое `HEAD`, `origin`, `upstream`?
- Как отменить последний коммит? Разница между `reset` и `revert`?
- Что такое semver и чем `^1.2.3` отличается от `~1.2.3`?
- Зачем нужен lock-файл (`package-lock.json`)?

### 2.5 Реляционные БД и SQL

#### Пререквизиты (утилиты, софт, библиотеки)
- PostgreSQL 16+
- DBeaver или pgAdmin
- SQL клиент (`psql`)
- ORM/Query Builder: Prisma или TypeORM

#### План практических занятий
- Спроектировать схему для интернет-магазина (users, products, orders, order_items).
- Написать `JOIN`, `GROUP BY`, `HAVING`, подзапросы, индексы.
- Использовать `EXPLAIN ANALYZE` для анализа плана запросов.
- Попробовать оконные функции: `ROW_NUMBER()`, `RANK()`, `PARTITION BY`.
- Добавить миграции и сид-данные.
- Реализовать транзакцию с откатом при ошибке.
- Понять N+1 проблему ORM и решить через eager loading.
- Изучить connection pooling: что такое `max`, `min`, `idleTimeout`; что происходит, если все соединения в пуле заняты.

#### План вопросов
- Что такое нормализация (1NF/2NF/3NF) и где возможна денормализация?
- Чем `INNER JOIN` отличается от `LEFT JOIN`?
- Что делает индекс и когда он вреден? [См. ответ A3](#a3-индексы)
- Что такое ACID? [См. ответ A4](#a4-acid)
- Что такое N+1 проблема и как её решить?
- Зачем нужны оконные функции и когда их использовать?
- Что такое connection pool и почему без него сервис под нагрузкой деградирует?

### 2.6 NoSQL и кэширование

#### Пререквизиты (утилиты, софт, библиотеки)
- Redis
- MongoDB (опционально)
- `ioredis` (npm пакет)

#### Структуры данных Redis

| Тип | Применение |
|------|-------------|
| Strings | Кэш JSON, счётчики, сессии |
| Hashes | Объекты пользователя, профили |
| Lists | Очереди, лента событий |
| Sets | Уникальные значения, теги |
| Sorted Sets | Лидерборды, рейтинги |
| Pub/Sub | Реальное время, нотификации |

#### План практических занятий
- Добавить cache-aside для endpoint списка товаров.
- Реализовать TTL и явную инвалидацию кэша при изменении данных.
- Сравнить время ответа до и после кэширования.
- Реализовать rate limiter на Redis.
- Попробовать pub/sub: публикация события при создании заказа.

#### План вопросов
- Когда выбирать SQL, а когда NoSQL?
- Для чего нужен Redis помимо кэширования?
- Какие риски у невалидного кэша? [См. ответ A5](#a5-стратегии-кэша)

### 2.7 Архитектура backend и слои приложения

#### Пререквизиты (утилиты, софт, библиотеки)
- Framework: Express
- Логгер: `pino` или `winston`
- Конфигурация через `.env` + `dotenv`
- Валидация: `joi` или `zod`

#### План практических занятий
- Разделить проект на слои: controller, service, repository.
- Вынести конфигурацию окружений (dev/test/prod).
- Добавить централизованный обработчик ошибок.
- Реализовать паттерн Dependency Injection вручную (без фреймворка).
- Добавить DTO-валидацию входящих данных (`joi` или `zod`).
- Создать middleware: логирование запросов, проверка auth-токена, обработка ошибок.

#### План вопросов
- Что такое слоистая архитектура?
- Где должна находиться бизнес-логика?
- Что такое middleware и в каком порядке они вызываются в Express?
- Что такое Dependency Injection и зачем он нужен?
- Что такое DTO и чем отличается от модели?
- Когда уместен монолит, а когда микросервисы? [См. ответ A6](#a6-монолит-vs-микросервисы)

### 2.8 Аутентификация и безопасность

#### Пререквизиты (утилиты, софт, библиотеки)
- JWT библиотека (`jsonwebtoken`)
- bcrypt/argon2
- Библиотека валидации
- OWASP Top 10 (как чеклист)
- `helmet`, `cors`, `express-rate-limit`

#### План практических занятий
- Реализовать регистрацию и логин.
- Хранить пароли только в виде хеша.
- Добавить refresh token и logout.
- Защитить endpoint по ролям (RBAC).
- Понять разницу между stateful (session + cookie) и stateless (JWT) аутентификацией.
- Изучить концепцию OAuth2/OpenID Connect (на уровне понимания схемы).
- Разобрать где хранить JWT: `localStorage` vs `httpOnly cookie` — плюсы/минусы в контексте XSS и CSRF.
- Реализовать отдачу JWT через `httpOnly` куку: `res.cookie("token", jwt, { httpOnly: true, secure: true, sameSite: "lax" })`.

#### План вопросов
- Чем authentication отличается от authorization?
- Почему нельзя хранить пароли в открытом виде?
- Как защищаться от SQL injection, XSS, CSRF? [См. ответ A7](#a7-базовая-безопасность)
- Чем session-based auth отличается от JWT? Плюсы/минусы каждого?
- Зачем нужен refresh token?
- Где лучше хранить JWT: в `localStorage` или `httpOnly cookie`? [См. ответ A14](#a14-где-хранить-jwt)

### 2.9 Тестирование

#### Пререквизиты (утилиты, софт, библиотеки)
- Jest
- Supertest
- `nyc` или встроенный coverage в Jest

#### План практических занятий
- Написать unit-тесты для сервисов (бизнес-логика, валидация).
- Написать интеграционные тесты для API и БД через Supertest.
- Настроить отчет по покрытию (`--coverage`).
- Создать мок для внешнего API (`jest.mock`, ручной мок).
- Настроить тестовую БД (отдельная от dev, очищается перед каждым прогоном).

#### План вопросов
- Чем unit тест отличается от интеграционного?
- Что такое мок и где его применять?
- Почему 100% coverage не гарантирует качество? [См. ответ A8](#a8-покрытие-тестами)
- Что такое пирамида тестов?
- Когда стоит мокать, а когда лучше использовать реальную БД?

### 2.10 Docker, CI/CD и деплой

#### Пререквизиты (утилиты, софт, библиотеки)
- Docker Desktop
- Docker Compose
- Nginx (базово)
- Облако/VPS (Render/Fly.io/Railway)
- GitHub Actions

#### План практических занятий
- Написать `Dockerfile` с multi-stage build (build + production).
- Настроить `.dockerignore` для исключения `node_modules`, `.git`.
- Докеризировать API + БД + Redis через docker-compose.
- Настроить volumes для персистентных данных БД.
- Добавить healthcheck и переменные окружения.
- Настроить GitHub Actions pipeline: lint → test → build → deploy.
- Поднять тестовый деплой на Render/Fly.io.

#### План вопросов
- Что такое образ и контейнер?
- Зачем multi-stage build?
- Какие переменные нельзя хранить в репозитории? [См. ответ A9](#a9-secrets-и-env)
- Что такое CI/CD pipeline и зачем он нужен?
- Что такое слои Docker-образа и как кэш сборки ускоряет деплой?

### 2.11 Наблюдаемость и эксплуатация

#### Пререквизиты (утилиты, софт, библиотеки)
- Логгер: `pino` или `winston`
- OpenTelemetry (база, опционально)
- Sentry (опционально)

#### План практических занятий
- Добавить структурированные JSON-логи с уровнями (`debug`, `info`, `warn`, `error`).
- Добавить `request_id` (correlation id) ко всем логам запроса через `AsyncLocalStorage` — без явной передачи в каждую функцию.
- Добавить базовые метрики: RPS, latency, error rate.
- Реализовать health-check endpoint (`GET /health`).
- Реализовать graceful shutdown (корректная остановка при SIGTERM).
- Добавить тайм-аут для вызовов внешних API (паттерн circuit breaker на концептуальном уровне): что такое, когда применять.
- Написать postmortem для вымышленного инцидента.

#### План вопросов
- Что логировать обязательно?
- Почему stack trace без контекста мало полезен?
- Как диагностировать рост latency? [См. ответ A10](#a10-диагностика-latency)
- Зачем нужен graceful shutdown?
- Что такое structured logging и почему он лучше простых строк?
- Что такое circuit breaker и когда он нужен?
- Что такое `AsyncLocalStorage` и зачем он нужен для request_id?

### 2.12 Подготовка к собеседованию

#### Пререквизиты
- Notion/Obsidian/Markdown для заметок
- Таймер для мок-интервью
- Список вопросов (в этом документе)

#### План практических занятий
- Каждую неделю 1 мок-собеседование на 45-60 минут.
- Разбор 20-30 типовых вопросов по backend.
- Короткий питч о каждом проекте: цель, архитектура, сложности, результаты.
- Подготовить 2-3 STAR-истории (Ситуация, Задача, Действие, Результат) для поведенческих вопросов.
- Потренироваться отвечать вслух (запись на диктофон / live coding с таймером).

#### План вопросов
- Как объяснить архитектуру своего проекта за 2-3 минуты?
- Как отвечать на вопрос «расскажи о сложной задаче, которую ты решал»?
- Что делать, если не знаешь ответ на технический вопрос? [См. ответ A11](#a11-как-отвечать-на-собесе)
- Какие вопросы задавать интервьюеру в конце?
- Какие компромиссы ты принял и почему?

---

## 3. Расписание по неделям (18 недель)

| Неделя | Модуль | Ключевые задачи |
|--------|--------|------------------|
| 1 | 2.1 HTTP REST | Поднять сервис `tasks`, реализовать полный CRUD |
| 2 | 2.1 HTTP REST + API design | Валидация, ошибки, CORS, пагинация, Swagger |
| 3 | 2.2 Async + Event Loop | Event loop, Promises, async/await, блокировка |
| 4 | 2.3 DSA | 6 задач (frequency map, two pointers, sliding window), Jest |
| 5 | 2.3 DSA | 6 задач (stack, sorting, recursion), разбор сложности |
| 6 | 2.4 Git | Ветки, merge/rebase, PR, conventional commits, npm |
| 7 | 2.5 PostgreSQL | Схема магазина, DDL, миграции, seed |
| 8 | 2.5 PostgreSQL | JOIN, GROUP BY, индексы, EXPLAIN ANALYZE, оконные функции |
| 9 | 2.6 Redis | Cache-aside, TTL, инвалидация, pub/sub |
| 10 | 2.7 Архитектура | Рефакторинг по слоям, config, DI, middleware, error handler |
| 11 | 2.8 Auth | Регистрация/логин, bcrypt, JWT access + refresh |
| 12 | 2.8 Auth + Security | RBAC, OWASP чеклист, helmet, rate-limit |
| 13 | 2.9 Тесты | Unit-тесты сервисов, моки, coverage |
| 14 | 2.9 Тесты | Интеграционные тесты API + БД через Supertest |
| 15 | 2.10 Docker + CI/CD | Dockerfile, docker-compose, GitHub Actions pipeline |
| 16 | 2.11 Observability | JSON-логи, request_id, health-check, graceful shutdown |
| 17 | 2.12 Интервью | 2 мок-собеседования, STAR-истории, питч проекта |
| 18 | 2.12 Интервью | Финальный мок, повторение слабых тем, подготовка резюме |

> Если нужно 12 недель — сокращай DSA (2→1 неделя), Git (параллельно с остальным), объединяй Redis+Архитектуру.
> Если нужно 24 недели — удваивай каждый блок, добавляй второй проект.

## 3.1 Как это применяется на фронте

- HTTP и статусы: фронт строит UX по статусам (`loading`, `error`, `empty`, `success`).
- Валидация: фронт и бэк должны использовать совместимые правила.
- Auth: фронт управляет access/refresh flow и состоянием сессии.
- Пагинация и фильтры: фронт зависит от контракта API (`page`, `limit`, `sort`, `filter`).
- Кэш: фронт (React Query/SWR) синхронизируется с серверным кэшем и инвалидацией.
- Наблюдаемость: `request_id` помогает связывать фронтовую ошибку с логами бэка.

## 4. Критерии готовности к собеседованию

- Есть 2-3 завершенных backend проекта в репозитории.
- Есть README с архитектурой, запуском, API примерами.
- Есть тесты (unit + integration).
- Уверенно объясняешь БД, API, безопасность, кэш, деплой.
- Можешь рассказать о реальных технических решениях и компромиссах.

## 5. Профиль подготовки (зафиксировано)

- Основной стек: `Node.js + Express`.
- NestJS: **отложен** до уверенного владения Express.
- Нагрузка: `5-7` часов в неделю.
- Позиционирование: backend, но с пониманием интеграции с фронтендом.
- Рынок: собеседования в компании среднего размера.

## 6. Краткие ответы

### A1 HTTP-статусы
- `200 OK`: успешный запрос с телом ответа.
- `201 Created`: создан новый ресурс.
- `204 No Content`: успех без тела ответа.
- `400 Bad Request`: некорректный запрос.
- `404 Not Found`: ресурс не найден.
- `409 Conflict`: конфликт состояния (например, дубликат).
- `422 Unprocessable Entity`: валидация не прошла.
- `500 Internal Server Error`: ошибка сервера.

### A2 Edge-cases
- Edge-cases выявляют баги на границах входных данных: пустые значения, `null`, длинные строки, переполнение, дубликаты.
- Без edge-case тестов система кажется рабочей, но падает в проде на редких сценариях.

### A3 Индексы

**Типы индексов в PostgreSQL:**
- **B-tree** (по умолчанию) — для `=`, `<`, `>`, `BETWEEN`, `LIKE 'abc%'`. Подходит для большинства случаев.
- **Hash** — только для `=`, не поддерживает диапазоны и сортировку.
- **GIN** — для массивов, JSONB, полнотекстового поиска.

**Составной индекс** `(a, b)` — используется запросами `WHERE a=?` и `WHERE a=? AND b=?`, но **не** `WHERE b=?` одним. Порядок колонок важен.

**Partial index** — индекс только по части строк: `CREATE INDEX ON orders(user_id) WHERE status='active'`. Меньше размер, быстрее обновление.

**Covering index** — покрывает все колонки запроса → index-only scan, таблица не читается.

Когда индекс **вреден**: малая селективность (булевый флаг), маленькая таблица, частые INSERT/UPDATE/DELETE.

`EXPLAIN ANALYZE` — смотри `Index Scan` vs `Seq Scan` и `actual time`.

### A4 ACID

| Свойство | Смысл |
|----------|-------|
| **Atomicity** | Транзакция выполняется целиком или откатывается полностью |
| **Consistency** | После транзакции все constraints выполнены, БД в допустимом состоянии |
| **Isolation** | Параллельные транзакции не видят промежуточных состояний друг друга |
| **Durability** | После COMMIT данные сохранены даже при сбое питания |

**Уровни изоляции** и допускаемые аномалии:

| Уровень | Dirty Read | Non-Repeatable Read | Phantom Read |
|---------|:---:|:---:|:---:|
| Read Uncommitted | ✓ | ✓ | ✓ |
| **Read Committed** (PG default) | ✗ | ✓ | ✓ |
| Repeatable Read | ✗ | ✗ | ✓ |
| Serializable | ✗ | ✗ | ✗ |

- **Dirty Read** — читаешь незакоммиченные изменения другой транзакции.
- **Non-Repeatable Read** — один `SELECT` возвращает разные значения в рамках транзакции (другая закоммитила изменение).
- **Phantom Read** — повторный `SELECT` видит новые строки (другая вставила и закоммитила).

### A5 Стратегии кэша
- Cache aside: приложение само читает/пишет кэш.
- Write-through: запись сразу в кэш и БД.
- TTL и инвалидация обязательны, иначе старые данные приводят к ошибкам.

### A6 Монолит vs микросервисы
- Монолит проще на старте: меньше операционной сложности.
- Микросервисы дают изоляцию и масштабирование, но повышают сложность инфраструктуры.
- Для Junior и первых проектов обычно достаточно модульного монолита.

### A7 Базовая безопасность

**SQL Injection** — никогда не конкатенируй ввод пользователя в строку запроса:
- Плохо: `` `SELECT * FROM users WHERE id = ${req.params.id}` ``
- Хорошо: `db.query("SELECT * FROM users WHERE id = $1", [req.params.id])`
- ORM защищают автоматически при правильном использовании.

**XSS (Cross-Site Scripting)** — вредоносный скрипт внедряется на страницу. Backend: `Content-Security-Policy` через `helmet`, не возвращать пользовательский HTML без санитизации. Хранение токенов в `HttpOnly` cookie — JS не сможет их украсть.

**CSRF** — сторонний сайт отправляет запрос от имени авторизованного пользователя. Защита: `SameSite=Lax/Strict` на cookie, CSRF-токен для форм. JWT в `Authorization` заголовке не страдает — браузер его не добавляет автоматически.

**Broken Access Control** (OWASP #1) — проверяй права на каждом endpoint, не только на UI. Пользователь A не должен получать/менять данные пользователя B.

**Rate limiting** — `express-rate-limit` или Redis-based лимитер. Защита от brute force и DDoS.

**Хранение паролей** — `bcrypt` (cost ≥ 12) или `argon2id`. Никогда: MD5, SHA1, plain text, обратимое шифрование.

### A8 Покрытие тестами
- Высокое покрытие не проверяет качество самих проверок.
- Можно иметь 100% coverage и не тестировать важные бизнес-сценарии.
- Нужны осмысленные тест-кейсы, включая негативные и интеграционные.

### A9 Secrets и env
- Никогда не коммитить API ключи, пароли, private keys.
- Использовать `.env` локально и секрет-хранилища в CI/CD и облаке.
- Добавить пример `.env.example` без секретов.

### A10 Диагностика latency
- Проверить: БД-запросы, индексы, сетевые вызовы, блокирующие операции.
- Сравнить p50/p95/p99 latency.
- Добавить трассировку запроса по слоям (API -> service -> DB/external).

### A11 Как отвечать на собесе
- Если не знаешь: проговорить гипотезу, уточнить вводные, предложить проверку.
- Важна структура: проблема -> варианты -> компромисс -> выбор.
- Не угадывать уверенно, лучше показать инженерное мышление.

### A12 Пагинация
- **Offset** (`OFFSET 20 LIMIT 20`): просто, но медленно на больших offset и нестабильно при вставках.
- **Cursor** (`WHERE id > 42 LIMIT 20`): быстро и стабильно, но нельзя перепрыгнуть на произвольную страницу.

### A13 Event Loop
- Node.js — однопоточный с неблокирующим I/O.
- Фазы: timers → pending I/O → poll → check → close.
- Между фазами: `process.nextTick()` → microtask (`Promise.then`) → следующая фаза.
- Порядок: `nextTick` → `Promise.then` → `setTimeout(0)` → `setImmediate`.

### A14 Где хранить JWT

| | `localStorage` | `httpOnly cookie` |
|--|:---:|:---:|
| **XSS-уязвимость** | Да — JS читает токен | Нет — JS не имеет доступа |
| **CSRF-уязвимость** | Нет | Да — нужен `SameSite=Lax/Strict` |
| **Удобство реализации** | Просто — `Authorization: Bearer` | Сложнее — управление cookie |
| **Мобильные / SPA** | Удобно | Сложнее |

**Рекомендация для веб-приложений:** `httpOnly; Secure; SameSite=Lax` cookie + refresh token. `SameSite=Lax` закрывает большинство CSRF-атак без CSRF-токена.
